manifest_version = "3"

[package]
name = "prime::diagram"
version = "0.1.0"

[libraries]
prime_diagram = { name = "prime::diagram", path = "diagram.prime", visibility = "pub" }
core_types = { name = "core::types", path = "../../core/types.prime", visibility = "pub" }
