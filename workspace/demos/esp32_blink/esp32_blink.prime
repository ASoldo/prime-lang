module demos::esp32_blink;

fn main() {
  out("hello from esp32 blink demo");

  let int32 led = 2;
  pin_mode(led, 1);
  loop {
    digital_write(led, 0);
    delay_ms(1000);
    digital_write(led, 1);
    delay_ms(1000);
  }
}
