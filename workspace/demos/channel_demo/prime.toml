manifest_version = "3"

[package]
name = "demos.channel_demo"
version = "0.1.0"

[module]
name = "demos.channel_demo"
path = "channel_demo.prime"
visibility = "pub"

[libraries]
core_types = { name = "core.types", path = "../../core/types.prime", visibility = "pub" }
