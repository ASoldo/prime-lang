module demos::interface_demo;

struct Hero {
  name: string;
}

interface Nameable {
  fn label(hero: Hero) -> string;
}

impl Nameable for Hero {
  fn label(hero: Hero) -> string {
    hero.name
  }
}

fn main() {
  let Hero hero = Hero{
    "Prime Hero",
  };
  let string method_label = hero.label();
  out(`Named hero: {method_label}`);
  out(`Method label: {method_label}`);
}
