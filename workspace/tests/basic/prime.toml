manifest_version = "3"

[package]
name = "tests::basic"
version = "0.1.0"

[dependencies]
pkg_lib = { name = "pkg::lib", path = "../../pkg/lib" }

[libraries]
core_types = { name = "core::types", path = "../../core/types.prime", visibility = "pub" }

[tests]
tests_basic = { name = "tests::basic", path = "basic.prime", visibility = "pub" }
