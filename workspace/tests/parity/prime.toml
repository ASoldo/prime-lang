manifest_version = "3"

[package]
name = "tests::parity"
version = "0.1.0"

[libraries]
core_types = { name = "core::types", path = "../../core/types.prime", visibility = "pub" }

[tests]
parity_tests = { name = "tests::parity", path = "parity.prime", visibility = "pub" }

