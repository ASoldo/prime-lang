import "pkg_lib";

fn main() {
  let Banner banner = banner("Prime modules loaded");
  out("Base banner:");
  out(banner.message);

  let Banner promoted = promote("Launch build", true);
  out("Promotion status:");
  out(describe(promoted));
}
